# Input tarballs — signpost

Moved. What the pipeline needs before it can run (the four input tarballs, the
GHG price file, how MAgPIE finds them through `cfg$repositories`, and the
tarballs packed between phases) is in [`tool-surface.md`](tool-surface.md)
section 4, "What you need before you can run".
